import React from 'react'
import styled from 'styled-components';

function Header() {
    return (
        <div>
           Header 
        </div>
    )
}
import styled from 'styled-components';

export default Header
